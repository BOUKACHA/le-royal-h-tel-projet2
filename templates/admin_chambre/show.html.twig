{% extends 'base-backend.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_chambre_index') }}" class="btn btn-dark">Retour à la liste</a>
    <h1>Chambre</h1>

    <table class="table table-index table-striped table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chambre.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ chambre.description }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ chambre.tarif }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ chambre.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ chambre.slug }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_admin_chambre_edit', {'id': chambre.id}) }}"class="btn btn-success"><i class="align-middle" data-feather="edit-2"></i></a>

    {{ include('admin_chambre/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}
<script src="{{ asset('js/add-item-link.js')}}"></script>
{% endblock %}
